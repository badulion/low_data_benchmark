model:
  _target_: src.model._point._gcn.GCN
  hidden_layers: 5
  input_size: ${InChannels}
  output_size: ${OutChannels}
  lookback: ${lookback}
  hidden_size: 1024
  #dropout: 0.1
  #knn: 16

structure: cloud
wrapper: ${graph_wrapper}
name: gcn